/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/main/resources/templates/**/*.html"], // Rất quan trọng: chỉ định quét các file HTML của Thymeleaf
    theme: {
        extend: {},
    },
    plugins: [],
}